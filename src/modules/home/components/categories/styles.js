
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    categoriesSlider : {
        marginTop: 24,
    },
    slideImage: {
        width: 50,
        margin: 'auto'
    },
    slideName: {
        textAlign: 'center',
        padding: 12
    }
}));

export default useStyles;